# hadolint configuration
# https://github.com/hadolint/hadolint#configure

ignored:
  # alpine の apk パッケージはバージョン固定するとセキュリティ更新の取りこぼしや
  # 将来の追従コストが大きくなるため、運用上は Dependabot によるベースイメージ
  # 更新側に任せ、本ルールは無視する。
  - DL3018
  # apt パッケージのバージョン固定も DL3018 と同じ理由で無視する。開発用
  # サンドボックスでは全パッケージの版固定はメンテコストが高く、セキュリティ
  # 更新の取りこぼしにつながるため、ベースイメージ更新側（Dependabot）に任せる。
  - DL3008
  # /root/.bashrc を生成する echo は既存の挙動（リテラル出力）を維持する必要が
  # あり、printf へ書き換えると生成内容が変わってしまう。info レベルの指摘の
  # ため、挙動不変を優先して本ルールは無視する。
  - SC2028
